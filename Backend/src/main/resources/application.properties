spring.application.name=SmartCampusApplication

# Railway MySQL (use environment variables)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/campus_db}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:haarshitha.30}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# IMPORTANT for Railway dynamic port
server.port=${PORT:8081}
